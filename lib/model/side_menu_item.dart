import 'package:flutter/material.dart';

class SideMenuItem {
  String title;
  IconData icon;
  Color background1, background2;

  SideMenuItem(this.title, this.icon, this.background1, this.background2);
}
